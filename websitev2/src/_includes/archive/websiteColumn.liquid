<div class="Archive-websiteColumn">
    <!-- header -->
    <span class="Archive-websiteColumnHeader">{{ website.data.name }}</span>
    <br><hr/>    
    
    <!-- articles -->
    {% if articles.size == 0 %}
        <p class="Archive-noArticleMsg">No articles found for this date.</p>
    {% else %}
        {% for article in articles %}
            <div class="Archive-article">

                <!-- thumbnail -->
                {% if article.thumbnail != null %}
                    <img class="Archive-articleThumbnail" src="{{ article.thumbnail }}"/>
                {% endif %}

                <!-- title -->
                <a href="{{ article.url }}"  class="Archive-articleTitle">{{ article.title }}</a>
                <br>        

                <!-- subtitle -->
                {% if article.subtitle != null %}
                    <div class="Archive-articleSubtitle">{{ article.subtitle }}</div>
                {% endif %} 

                <!-- author -->
                {% if article.author != null %}
                    <span class="Archive-articleAuthor">{{ article.author }}</span>
                {% endif %}
                
            </div>
        {% endfor %}
    {% endif %}
</div>